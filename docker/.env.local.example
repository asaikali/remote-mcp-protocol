# Local Environment Configuration Example
# Copy this file to .env.local and customize for your local development
# 
# cp .env.local.example .env.local
# 
# The .env.local file is gitignored, so your local changes won't be committed.
# Values in .env.local override values in .env (team defaults).

# =============================================================================
# MCP Services - Uncomment and modify any ports you want to override locally
# =============================================================================

# SSE Server port (Server-Sent Events transport)
# MCP_SSE_PORT=5001

# Streamable HTTP Server port (HTTP streaming transport)  
# MCP_STREAMABLE_PORT=5002

# MCP Inspector GUI port (Web interface)
# MCP_INSPECTOR_PORT=7274

# MCP Inspector WebSocket port (WebSocket proxy)
# MCP_INSPECTOR_WS_PORT=7277

# =============================================================================
# PostgreSQL Services - Uncomment and modify any ports you want to override locally
# =============================================================================

# PostgreSQL database port
# PG_PORT=25432

# pgAdmin web interface port
# PGADMIN_PORT=25433

# =============================================================================
# Observability Services - Uncomment and modify any ports you want to override locally
# =============================================================================

# Grafana dashboard port (Web interface)
# GRAFANA_PORT=8000

# Prometheus metrics server port (Web interface)
# PROMETHEUS_PORT=8090

# Loki log aggregation port (HTTP API)
# LOKI_PORT=8100

# Tempo distributed tracing port (OTLP HTTP API)  
# TEMPO_PORT=8200

# Tempo Zipkin compatibility port (Zipkin HTTP API)
# TEMPO_ZIPKIN_PORT=8411

# OpenTelemetry Collector gRPC port (OTLP gRPC API)
# OTEL_COLLECTOR_GRPC_PORT=8317

# OpenTelemetry Collector HTTP port (OTLP HTTP API)
# OTEL_COLLECTOR_HTTP_PORT=8318

# =============================================================================
# Common Scenarios
# =============================================================================

# Example: If MCP port 3001 is busy on your Mac:
# MCP_SSE_PORT=8001

# Example: If PostgreSQL port 15432 is busy:
# PG_PORT=25432
# PGADMIN_PORT=25433

# Example: If Grafana port 3000 is busy (common conflict):
# GRAFANA_PORT=8000
# PROMETHEUS_PORT=8090
# LOKI_PORT=8100

# Example: Move all services to 5000s range:
# MCP_SSE_PORT=5001
# MCP_STREAMABLE_PORT=5002
# MCP_INSPECTOR_PORT=5274
# MCP_INSPECTOR_WS_PORT=5277
# PG_PORT=5432
# PGADMIN_PORT=5433
# GRAFANA_PORT=5000
# PROMETHEUS_PORT=5090